# CLAUDE.md

## 回答方針

- 日本語で回答すること。

## 実装方針

- **非破壊優先**: 実装済みの機能を破壊しない、または破壊リスクの少ない実装方法を優先する。
  - 実現が難しい場合は、その旨を回答したうえで、破壊的な実装方法を「提案のみ」行い、実装は進めない。
- **可読性重視**: 人間が見ても把握しやすいコーディングを心がける。

---

## 機能概要

### 対象ページ

```
https://live.fc2.com/*
```

FC2ライブの放送視聴ページ上で動作するChrome拡張機能。

---

### 主要機能

#### 1. マルチビュー表示領域

- ブラウザウィンドウ**下部**に、最大4放送分の小画面を表示するための領域を設ける。
- 初期状態では「＋」のような「放送を追加する旨を示す表示」を表示する。

#### 2. 放送の追加（URLドラッグ＆ドロップ）

- 「＋」の領域にURLをドラッグすると、該当放送の映像を小画面で表示する。
  - 小画面のサイズは下部領域の縦幅に収まるサイズとする。
  - 小画面が追加された後も、空き枠には「＋」の領域が残る。

#### 3. 小画面の削除

- 各小画面の**右上に「×」ボタン**を表示する。
- 「×」ボタン押下で、該当小画面を閉じる（その枠は「＋」に戻る）。

#### 4. メイン表示との入れ替え

- 小画面をクリックすると、その放送を**メイン表示（大画面）に切り替える**。
- 元のメイン表示の放送は、クリックされた小画面の枠に移動する（**スワップ**）。

---

### 制約・仕様まとめ


| 項目        | 内容                  |
| --------- | ------------------- |
| 最大同時表示数   | メイン1 ＋ 小画面最大4（計5放送） |
| 小画面追加方法   | URLのドラッグ＆ドロップ       |
| 小画面削除方法   | 右上「×」ボタン            |
| メイン切り替え方法 | 小画面をワンクリック          |
| 動作タブ      | 同一タブ内で完結（新規タブ不要）    |


